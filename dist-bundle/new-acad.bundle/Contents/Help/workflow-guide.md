# new-acad 工作流说明

> 写给不懂代码的人看
> 目标是让 AI 帮你读图纸、做分析

---

## 一、一句话说清楚我们在做什么

**选中图元 → 面板发消息 → 汉克帮你操作 CAD。**

我们做了一套面板（嵌在 C3D 里）+ 后台（汉克），让你在面板里打字就能操作图纸。

---

## 二、怎么用

### 准备工作（一次性的）

1. **C3D 已自动加载插件** — 正常启动 C3D 就会自动 `NETLOAD` 面板插件
2. **Relay 自动启动** — Windows 登录时自动跑面板中转服务
3. 面板在 C3D 右下角弹出来，状态栏显示 **在线**

### 日常使用

```
在 CAD 里选中图元 → 切换到面板 → 打字发消息 → 汉克操作 CAD → 结果回面板
```

### 操作示例

| 你说 | 汉克做 |
|------|--------|
| "选中图元变成红色" | 调 `setEntityColor` 变色 |
| "我选了什么东西" | 调 `getEntityInfo` 读类型、图层 |
| "在 0,0 画根 2000 的线" | 调 `createPolyline` 画图 |
| "帮我算曲面面积" | 调 `getSurface` 查数据 |
| "做个 3m 路肩 + 1:4 放坡的部件" | 自动生成 SAC 道路部件并导入 C3D（8-16 新增） |

### 项目文件夹（可选，多工程管理）

配置区最下面一行「项目名」：
- **下拉选已有项目 = 直接切换**；输新名字 + 点「建立」= 新建并切换
- 「打开」= 资源管理器看项目文件夹；「清理」= 删项目全部数据（确认后不可恢复）
- **不填 = 全局模式**，所有图纸数据放一起（和以前一样）
- 作用：AI 的临时文件、选择记忆按项目隔开，同时干几个工程互不串（8-17 新增）

---

## 三、能做什么不能做什么

### ✅ 能做的
- 选中图元 → 查属性、变色、改图层
- 画线、画圆、创建曲面
- 读取曲面/路线/廊道数据
- 查询曲面面积、高程统计
- 生成工程量清单
- 读取/导出文件（Word/Excel/CSV/报告，安全限制内）
- **描述需求 → 自动生成道路部件**（SAC，8-16 新增）
- **按项目隔离数据**（项目文件夹，8-17 新增）

### ❌ 不能做的
- 做设计决策（AI 不替你判断"这路要不要转弯"）
- 自动导出计算书
- 同时处理多张图纸的操作（可切换图纸，但选择集按图纸区分：切图后需在新图纸重新框选）

---

## 四、出问题了怎么办

| 现象 | 解决 |
|------|------|
| 面板不显示 | C3D 命令行输 `HANK_SHOW` |
| 面板显示"离线" | relay 没启动 → 双击安装目录 `server\start-relay.bat`（或重开 C3D 自动拉起） |
| 汉克说"连接不到 CAD" | 检查 C3D 是否打开，插件是否加载 |
| C3D 崩溃 | 重启 C3D，自动恢复 |
| 选中图元但汉克说读不到 | 发消息时已自动捕获；仍读不到 → ESC 取消选择，重新框选后再发消息 |

---

## 五、一句话记住

**打开 C3D → 框选 → 面板聊天。** 剩下的汉克搞定。

---

## 图片 / 扫描稿 → CAD（v1.6.0）

用户给图（截图 / 扫描件 / 手绘草图 / 照片）要求"画进 CAD"时，**先分诊，再决定画法**：

| 图里有什么 | 怎么做 | 产物 |
|---|---|---|
| **有尺寸标注** | 先列尺寸表（每段/总长/门窗）→ 尺寸链闭合自检 → 不确定项问用户 → 按真实尺寸画（锚点+相对尺寸）→ 回读对账 | 可施工的真尺寸图 |
| 无标注但有标定线索（比例尺/网格/坐标标注） | 用线索标定后重建 | 可施工（略降级） |
| **完全没有尺寸** | **只问一次**："图上有没有哪一段长度你知道？哪怕一个。"三选项：①按图比例画 ②给一个参考尺寸（单点标定）③给一个目标总尺寸 | 比例图 / 示意图 |
| 不是工程图（照片/实物/插画） | 照形描（traceImage）或按目标总尺寸缩放，**如实说明是示意图** | 示意图 |

**铁律**：坐标只来自「图上的标注 / 用户给的尺寸」；像素几何只用于识别形状与拓扑；**绝不硬猜尺寸**。

**照形描（traceImage）模式选择**：不传 mode 自动判定（扁平/纯色/卡通/logo → flat；线稿/手绘/照片 → arc）；要落地直接 `draw:true`（工具自己建图层、批量画完报成功/失败）。

详见 `knowledge/image-to-cad.md`。